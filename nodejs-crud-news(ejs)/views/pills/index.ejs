<!DOCTYPE html>
<html lang = "en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medicine</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous"></head>
<body class = "container" style = "margin-top: 50px;">

    
    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>
    
    <% if (messages.success) { %>
        <div class="alert alert-success" role="alert"><%= messages.success %></div>
    <% } %>


    <a href = "/" class = "btn btn-info">Back</a>

    <div class = "card">
        <div class = "card-header">
            <ul class = "nav nav-pills w-100">
                <li class = "nav-link">Medicine</li>
                <li class = "nav-link ml-auto">
                    <a href = "/pills/add" class = "nav-link active">Add Medicine</a>
                </li>
            </ul>
        </div>
        <div class = "card-body">
            <% if(data.length) { %>
                <table class = "table">
                    <thead>
                        <tr>
                            <th scope = "col"> # </th>
                            <th scope = "col"> Name </th>
                            <th scope = "col"> Author </th>
                            <th scope = "col"> Detail </th>
                            <th scope = "col"> date </th>
                            <th width = "300px"> Action </th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let i = 0; i < data.length; i++) { %>
                            <tr>
                                <th scope = "row"><%= data[i].id%></th>
                                <td><%= data[i].name%></td>
                                <td><%= data[i].author%></td>
                                <td><%= data[i].details%></td>
                                <td><%= data[i].update_at%></td>
                                <td>
                                    <a href="../pills/edit/<%= data[i].id%>" class = "btn btn-warning edit">Edit</a>
                                    <a href="../pills/delete/<%= data[i].id%>" onclick="return alert('Are you sure?')" class = "btn btn-danger delete">Delete</a>
                                    <a href="../pills/detail/<%= data[i].id%>" class = "btn btn-success detail">Detail</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            <% } %> 

            <!-- if result is empty -->
            <% if(!data.length) { %>
                <p class = "text-center">No News found!</p>
            <% } %>
        </div>
    </div>


</body>
</html> 
